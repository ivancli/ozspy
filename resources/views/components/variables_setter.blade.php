@if ($errors->any())
    {{dump($errors)}}
    {{dump($errors->all())}}
    <div class="alert alert-danger">
        <ul>
            @foreach ($errors->all() as $error)
                <li>{{ $error }}</li>
            @endforeach
        </ul>
    </div>
@endif
